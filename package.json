{
  "name": "discord-nsfw-raw-api-bot",
  "version": "2.0.0",
  "description": "NSFW Discord bot implemented directly against the raw Discord HTTP API and Gateway.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "register": "node src/register-commands.js",
    "check": "node --check src/index.js && node --check src/register-commands.js && node --check src/discord.js && node --check src/media.js && node --check src/commands.js"
  },
  "engines": { "node": ">=18.17" },
  "dependencies": { "ws": "^8.18.0" }
}
